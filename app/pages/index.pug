extends ../layouts/default
include ../blocks/branding/branding
include ../blocks/index/index
include ../blocks/filter-sale/filter-sale

block head
  - var pageTitle = 'Катера.RU - Главная'

block contentVariables
  - var page = 'index';

block content
  div(style="width: 100vw; min-height: 100%; background-color: #fff; position: absolute; top: 0; left: 0; z-index: 9999; padding: 50px 0;")
    +container()
      div(style="width: 850px")
        +filter-sale
  +branding('index')
    +index()
