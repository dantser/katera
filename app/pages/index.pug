extends ../layouts/default
include ../blocks/branding/branding
include ../blocks/index/index

block head
  - var pageTitle = 'Катера.RU - Главная'

block contentVariables
  - var page = 'index';

block content
  div(style="width: 100vw; height: 100vh; position: fixed; top: 0; left: 0; z-index:9999;  background-color: #fff")
    +container()
  +branding('index')
    +index()

