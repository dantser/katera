mixin subscribe()
  +b.subscribe
    +e('h2').heading Подпишитесь на рассылку
    +e('form').form
      +e.section
        +e('input').input(type='text', value='Введите email')
      +e.section
        +e('button').btn Отправить
         +e('i').icon

