include ../upload-btn/upload-btn

mixin upload-form(label, supText)
  +b.upload-form&attributes(attributes)
    if (label)
      +e.SPAN.label= label
    +e.LABEL.field
      +e.INPUT(type='file' name='test').file
      +text-field({type:"text"})._dashed
      +upload-btn()
      if (supText)
        +e.SPAN.sup-text= supText
