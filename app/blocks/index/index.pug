include ../header/header
include ../article-preview/article-preview

mixin index()
  +b.index&attributes(attributes)
    +header()
    //- файл test-article-preview.json находится в app/data/test-article-preview.json
    +e('div')(style='width: 30%; position: relative')
      +article-preview(getData('test-article-preview'))
      +article-preview(getData('test-article-preview'))(class='_overlay')

