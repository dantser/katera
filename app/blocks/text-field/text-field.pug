//- ИНФОРМАЦИЯ ОБ ИСПОЛЬЗОВАНИИ ТЕКСТОВОГО ПОЛЯ В ./text-field.md
include ../icon/icon

mixin text-field(data)
  +b.text-field.js-text-field&attributes(attributes)
    +e('input')(type=data.type || 'text' name=data.name value=data.value || '').control
    +e('span').placeholder
      = data.placeholder
    +e.error-icon-wrapper
      +icon('exclamation-point').error-icon
