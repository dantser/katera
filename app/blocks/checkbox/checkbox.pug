include ../icon/icon
//- Аргументы
//- data - объект
//- data.name - имя чекбокса
//- data.value - значение чекбокса
//- data.checked - состояние checked чекбокса

//- Модификаторы
//- _switcher, делает из чекбокса, свитчер
//- _circle, делает чекбокс круглым (как в свитчере), только не без передвижений

//- Размеры чекбокса фиксированные, если будет необходимость менять размеры, нужно добавлять модификаторы
//- _sm, _big и.т.д
mixin checkbox(data)

  +b('label').checkbox&attributes(attributes)
    - var params = data || {}
    - var name = params.name || ''
    - var value = params.value || ''
    - var checked = params.checked || false

    +e('input')(type='checkbox' name=name value=value checked=checked).control
    +e.box
      +icon('check-mark').mark
